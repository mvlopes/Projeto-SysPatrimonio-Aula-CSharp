@model SysPatrimonio.Models.DbCategoria

@{
    ViewData["Title"] = "Criar Categoria";
}

<h1>Criar Categoria</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label class="control-label">Descrição Categoria</label>
                <input asp-for="descricaocategoria" class="form-control" />
                <span asp-validation-for="descricaocategoria" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Criar Categoria" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Voltar</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
